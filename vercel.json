{
  "buildCommand": "cd robotic-testing-frontend && npm install && npm run build",
  "outputDirectory": "robotic-testing-frontend/dist",
  "installCommand": "echo 'Using build command'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
